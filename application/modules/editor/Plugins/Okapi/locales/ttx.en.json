{
    "segmentMode": "Extraction mode",
    "segmentMode0": "Auto-detect existing segments (If found: extract only those, otherwise extract all)",
    "segmentMode1": "Extract only existing segments",
    "segmentMode2": "Extract all (existing segments and unsegmented text parts)",
    "escapeGT": "Escape greater-than characters in output"
}